@model bool

@if (Model)
{
    <i class="glyphicon glyphicon-list registered-more-than-once" 
       data-toggle="tooltip" 
       data-placement="right" 
       title="@Messages.Activity_Status_RegisteredMoreThanOnce"></i>
}